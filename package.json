{
  "main": "main.ts",
  "scripts": {
    "start": "deno run --allow-all server/mod.ts pkgs",
    "build": "tstl && yarn post-build",
    "post-build": "deno run --allow-all server/postBuild.ts"
  },
  "devDependencies": {
    "@jackmacwindows/lua-types": "^2.13.1",
    "@jackmacwindows/typescript-to-lua": "^1.12.0",
    "typescript": "^4.9.5"
  }
}